<h1>Listing user_stories</h1>

<table>
  <tr>
    <th>Story type</th>
    <th>Current state</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_stories.each do |user_story| %>
  <tr>
    <td><%= user_story.story_type %></td>
    <td><%= user_story.current_state %></td>
    <td><%= user_story.name %></td>
    <td><%= link_to 'Show', user_story %></td>
    <td><%= link_to 'Edit', edit_user_story_path(user_story) %></td>
    <td><%= link_to 'Destroy', user_story, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to 'New User story', {:controller => "user_stories", :action => "new", :id => @project.pivotal_id}, :class => 'clean-gray'%>
<%= button_to 'Back', {:controller => "projects", :action => "show", :id => @project.id}, :class => 'clean-gray' %>
