<table>
  <tr>
    <th>Projectos</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td class="first_bigger"><%= project.name %></td>
    <td><%= link_to 'Show', :controller => "projects", :action => "get_related_pages", :id => project.id  %></td>
    <td><%= link_to 'Edit', edit_project_path(project) %></td>
  
    <td><%= link_to 'Destroy', project, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to 'New Project', {:controller => "projects", :action => "new"}, :class => 'clean-gray' %>


